{
  "name": "ovo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "gen": "node -e 'require(\"./index\").gen()'",
    "clean": "node -e 'require(\"./index\").clean()'",
    "list": "node -e 'require(\"./index\").list()'",
    "spouse": "node -e 'require(\"./index\").spouse()'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },  
  "author": "",
  "license": "ISC",
  "dependencies": {
    "googleapis": "^27.0.0"
  }
}
